import { Context, PermissionsFlagsBits } from "../deps.ts";

export default {
  data: {
    name: "config",
    description: "Utak atik dulu g sih 🥦",
    dm_permission: false
  },
  async execute(ctx: Context): void {

  }
}
